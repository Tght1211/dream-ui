---
description: Dream UI - Glassmorphism 玻璃拟态 UI 风格规范，用于指导所有前端组件开发保持一致的视觉风格
globs: ["src/**/*.vue", "src/**/*.ts", "src/**/*.scss", "src/**/*.css"]
---

# Dream UI - Glassmorphism 玻璃拟态设计规范

## 〇、项目概览

- **npm 包名**：`@tght_1211/dream-ui`（已发布至 npm）
- **版本**：1.0.1+
- **技术栈**：Vue 3.5+ / TypeScript 5.9+ / Vite 7.x / Sass 1.97+
- **组件总数**：30+（基础 / 表单 / 数据展示 / 反馈 / 导航 / 特效 六大分类）
- **核心特性**：玻璃拟态 + 动态背景 + 梦幻蒙版 + 黑柔镜 + 每个组件可自定义透明度/边框色/光晕色

## 一、设计理念

本项目 UI 风格灵感来源于 AR/VR 空间界面设计，核心特征为 **玻璃拟态 (Glassmorphism)**，即：

- **极高透明度毛玻璃背景** — 组件背景几乎全透明（3-7%白色），磨砂玻璃质感
- **强烈的白色边框光晕** — 边框是核心视觉亮点，白色发光线条，多层 box-shadow
- **背景模糊穿透** — 透过组件可清晰看到背后内容
- **柔和的投影与层次** — 通过多层 shadow 营造空间深度感
- **纯暗色背景 + 纯白色调** — 深黑背景，白色文字和边框，颜色只用于图标点缀
- **不使用彩色主题** — 组件本体始终是纯白光晕，彩色只出现在图标和状态指示器中

## 二、色彩体系

### 2.1 背景色（极高透明度）

```css
--dream-bg-primary: rgba(255, 255, 255, 0.04);      /* 主要背景 - 几乎全透明 */
--dream-bg-secondary: rgba(255, 255, 255, 0.07);     /* 次要背景/悬停态 */
--dream-bg-elevated: rgba(255, 255, 255, 0.10);      /* 高亮/激活态 */
--dream-bg-deep: rgba(0, 0, 0, 0.25);                /* 深层背景 */
--dream-bg-overlay: rgba(0, 0, 0, 0.55);             /* 遮罩层 */
```

**重要**：背景透明度必须非常低（0.04-0.10），实现真正的"玻璃穿透"感。

### 2.2 文字色

```css
--dream-text-primary: rgba(255, 255, 255, 0.95);     /* 主文字 */
--dream-text-secondary: rgba(255, 255, 255, 0.60);   /* 次要文字 */
--dream-text-tertiary: rgba(255, 255, 255, 0.35);    /* 辅助文字 */
--dream-text-disabled: rgba(255, 255, 255, 0.20);    /* 禁用文字 */
```

### 2.3 点缀色（仅用于图标/状态指示器，不用于组件本体）

```css
--dream-color-primary: #60a5fa;    /* 天蓝 */
--dream-color-success: #4ade80;    /* 翠绿 */
--dream-color-warning: #fbbf24;    /* 琥珀 */
--dream-color-danger: #f87171;     /* 珊瑚红 */
--dream-color-info: #a78bfa;       /* 薰衣草紫 */
```

### 2.4 边框色

```css
--dream-border-default: rgba(255, 255, 255, 0.18);   /* 默认边框 - 比之前更亮 */
--dream-border-hover: rgba(255, 255, 255, 0.35);     /* 悬停边框 */
--dream-border-active: rgba(255, 255, 255, 0.50);    /* 激活边框 */
```

## 三、核心视觉效果（必须遵守）

### 3.1 毛玻璃效果

每个组件容器**必须**包含以下属性：

```css
backdrop-filter: blur(24px) saturate(1.1);
-webkit-backdrop-filter: blur(24px) saturate(1.1);
background: rgba(255, 255, 255, 0.04);  /* 极低透明度 */
```

blur 值参考：
- 轻度模糊：`blur(12px)` — 标签、徽章等小型组件
- 标准模糊：`blur(24px)` — 卡片、面板、Widget
- 深度模糊：`blur(40px)` — 对话框、下拉菜单

### 3.2 边框光晕效果（核心特征 — 必须多层叠加）

这是本设计系统最关键的视觉特征。参考真实 VR 界面，边框需要有明显的白色"发光线条"感：

```css
/* 基础边框 */
border: 1px solid rgba(255, 255, 255, 0.18);

/* 多层白色光晕 — 由内到外 */
box-shadow:
  /* 第1层：内部环境光 */
  inset 0 0 20px rgba(255, 255, 255, 0.04),
  /* 第2层：顶部边缘高光线 */
  inset 0 1px 0 rgba(255, 255, 255, 0.12),
  /* 第3层：外边缘紧贴发光（最关键） */
  0 0 1px 0 rgba(255, 255, 255, 0.20),
  /* 第4层：近距离柔和扩散 */
  0 0 15px rgba(255, 255, 255, 0.04),
  /* 第5层：远距离朦胧光晕 */
  0 0 30px rgba(255, 255, 255, 0.02),
  /* 第6层：底部投影 */
  0 8px 32px rgba(0, 0, 0, 0.25);
```

### 3.3 悬停态增强光晕

```css
&:hover {
  border-color: rgba(255, 255, 255, 0.35);
  box-shadow:
    inset 0 0 30px rgba(255, 255, 255, 0.06),
    inset 0 1px 0 rgba(255, 255, 255, 0.20),
    0 0 1px 0 rgba(255, 255, 255, 0.35),
    0 0 20px rgba(255, 255, 255, 0.08),
    0 0 50px rgba(255, 255, 255, 0.03),
    0 12px 40px rgba(0, 0, 0, 0.30);
  transform: translateY(-2px);
}
```

### 3.4 图标专属发光效果

图标使用圆形玻璃容器 + 对应颜色的发光：

```css
.icon-container {
  background: rgba(COLOR_RGB, 0.10);
  border: 1px solid rgba(COLOR_RGB, 0.20);
  border-radius: 50%;
  box-shadow:
    inset 0 0 10px rgba(COLOR_RGB, 0.05),
    0 0 1px rgba(COLOR_RGB, 0.20),
    0 0 12px rgba(COLOR_RGB, 0.08);
}
```

### 3.5 圆角规范

```css
--dream-radius-sm: 8px;      /* 小型组件 */
--dream-radius-md: 12px;     /* 中型组件 */
--dream-radius-lg: 16px;     /* 大型组件 */
--dream-radius-xl: 24px;     /* 对话框 */
--dream-radius-full: 9999px; /* 头像、图标 */
```

## 四、组件自定义系统（useGlassStyle）

### 4.1 通用自定义 Props

**每个玻璃拟态组件都必须支持以下 3 个可选属性**，通过 `useGlassStyle` composable 统一实现：

```ts
interface GlassCustomProps {
  bgOpacity?: number      // 背景透明度 0~1
  borderColor?: string    // 自定义边框颜色
  glowColor?: string      // 自定义光晕颜色
}
```

### 4.2 实现方式

在组件中使用 `useGlassStyle` composable：

```vue
<template>
  <div class="d-glass-xxx" :style="glassVars">
    <slot />
  </div>
</template>

<script setup lang="ts">
import { useGlassStyle, type GlassCustomProps } from '../../composables/useGlassStyle'

interface Props extends GlassCustomProps {
  // 组件自身 props...
}

const props = defineProps<Props>()
const { glassVars } = useGlassStyle(props)
</script>

<style scoped lang="scss">
.d-glass-xxx {
  /* 使用 CSS 变量并提供默认值 fallback */
  background: var(--_glass-bg, rgba(255, 255, 255, 0.04));
  border: 1px solid var(--_glass-border, rgba(255, 255, 255, 0.18));
  box-shadow:
    inset 0 0 20px rgba(255, 255, 255, 0.04),
    0 0 1px 0 var(--_glass-glow, rgba(255, 255, 255, 0.20)),
    0 0 15px var(--_glass-glow, rgba(255, 255, 255, 0.04)),
    0 8px 32px rgba(0, 0, 0, 0.25);
}
</style>
```

## 五、浮动内容规范（Teleport + 定位）

### 5.1 问题与方案

Select 下拉、Popover 弹出框等浮动内容会被父容器 `overflow: hidden` 裁剪。
**必须使用 `<Teleport to="body">` 将浮动内容渲染到 body**，并通过 `getBoundingClientRect()` 动态定位。

### 5.2 浮动内容也必须是玻璃拟态

浮动弹出层（Select dropdown / Popover / Drawer）**禁止使用纯黑背景**，必须同样使用：

```css
background: rgba(255, 255, 255, 0.06);
backdrop-filter: blur(40px) saturate(1.1);
-webkit-backdrop-filter: blur(40px) saturate(1.1);
```

### 5.3 Z-index 层级体系

```css
--dream-z-dropdown: 100;   /* Select 下拉、Popover 弹出 */
--dream-z-sticky: 200;     /* Navbar 导航栏（最高层） */
--dream-z-modal: 300;      /* Dialog 对话框、Drawer 抽屉 */
```

**关键规则**：Navbar 必须始终在最高可视层级，Select/Popover 的 z-index 不能超过 Navbar。

## 六、特效组件体系

### 6.1 动态背景（DBackgroundLayer）

提供 18 种内置背景：`neon`（默认）、`liquid`、`aurora`、`lava`、`ocean`、`sunset`、`forest`、`candy`、`midnight`、`warm`、`grid-white`、`grid-dark`、`dots`、`pure-dark`、`pure-blue`、`pure-purple`、`pure-green`、`pure-red`、`pure-white`。

### 6.2 梦幻蒙版（DDreamVeil）

灵感来自日漫/Persona 5 Royal 的白色柔光蒙版。全屏半透明白色覆盖 + 光斑粒子。
- 3 档强度：`subtle` / `medium` / `strong`
- 可选光斑粒子 `:particles="true"`

### 6.3 黑柔镜（DBloomVeil）

模拟 Tiffen Black Pro-Mist 滤镜效果，只让亮区产生柔和光晕扩散，暗区不受影响。
- 原理：`backdrop-filter: blur() brightness()` + `mix-blend-mode: screen`
- 双层结构：近距高光 glow + 远距柔和 spread
- 3 档强度：`subtle` / `medium` / `strong`

### 6.4 全局状态管理（useBackground）

所有背景和蒙版通过 `useBackground()` composable 统一管理：

```ts
const {
  currentBg, setBg,                           // 背景
  veilEnabled, toggleVeil, setVeilIntensity,  // 梦幻蒙版
  veilParticles, toggleVeilParticles,         // 蒙版光斑
  bloomEnabled, toggleBloom, setBloomIntensity, // 黑柔镜
} = useBackground()
```

## 七、排版规范

### 7.1 字体

```css
--dream-font-family: 'Inter', 'SF Pro Display', -apple-system, BlinkMacSystemFont, sans-serif;
```

### 7.2 字号 / 字重 / 间距

参见 `src/styles/variables.scss` 中的完整定义。

## 八、动效规范

### 8.1 过渡

所有交互状态变化**必须**有过渡动画：

```css
--dream-transition-fast: 0.15s ease;
--dream-transition-base: 0.25s ease;
--dream-transition-slow: 0.35s ease;
```

### 8.2 光晕脉冲动画（用于需要吸引注意力的组件）

```css
@keyframes dream-glow-pulse {
  0%, 100% { box-shadow: /* 标准光晕 */; }
  50% { box-shadow: /* 增强光晕，白色发光强度翻倍 */; }
}
```

## 九、页面背景规范

```css
body {
  background: #0a0c10;  /* 纯暗黑色，不要紫色/蓝色渐变 */
}
```

可添加极微弱的白色光源装饰：

```css
.bg-orb {
  background: radial-gradient(circle, rgba(255, 255, 255, 0.03), transparent 60%);
  filter: blur(120px);
}
```

## 十、组件开发规范

### 10.1 命名规范

- 组件名前缀：`DGlass`（交互组件）/ `D`（特效组件如 `DBackgroundLayer`、`DDreamVeil`、`DBloomVeil`）
- CSS 类名前缀：`d-glass-` / `d-bg-` / `d-dream-veil` / `d-bloom-veil`
- CSS 变量前缀：`--dream-`（全局设计令牌）/ `--_glass-`（组件级自定义变量）

### 10.2 组件结构模板

```vue
<template>
  <div :class="['d-glass-xxx']" :style="glassVars">
    <slot />
  </div>
</template>

<script setup lang="ts">
/**
 * @description 组件描述
 * @author buchi
 * @since 2026-02-08
 */
import { useGlassStyle, type GlassCustomProps } from '../../composables/useGlassStyle'

defineOptions({ name: 'DGlassXxx' })

interface Props extends GlassCustomProps {
  // 组件特有 props
}

const props = defineProps<Props>()
const { glassVars } = useGlassStyle(props)
</script>

<style scoped lang="scss">
.d-glass-xxx {
  backdrop-filter: blur(24px) saturate(1.1);
  -webkit-backdrop-filter: blur(24px) saturate(1.1);
  background: var(--_glass-bg, var(--dream-bg-primary));
  border: 1px solid var(--_glass-border, var(--dream-border-default));
  border-radius: var(--dream-radius-lg);
  color: var(--dream-text-primary);
  transition: all var(--dream-transition-base);

  /* 核心光晕 — 使用 --_glass-glow 实现可自定义 */
  box-shadow:
    inset 0 0 20px rgba(255, 255, 255, 0.04),
    inset 0 1px 0 rgba(255, 255, 255, 0.12),
    0 0 1px 0 var(--_glass-glow, rgba(255, 255, 255, 0.20)),
    0 0 15px var(--_glass-glow, rgba(255, 255, 255, 0.04)),
    0 0 30px rgba(255, 255, 255, 0.02),
    0 8px 32px rgba(0, 0, 0, 0.25);
}
</style>
```

### 10.3 npm 包相关

- 包名：`@tght_1211/dream-ui`
- 库入口：`src/lib.ts`
- 库构建配置：`vite.config.lib.ts`
- 类型声明配置：`tsconfig.lib.json`
- 构建命令：`npm run build:lib`
- 发布命令：`npm publish --access public`
- 新增组件后必须在 `src/components/dream-ui/index.ts` 和 `src/lib.ts` 中导出

## 十一、完整组件列表

| 分类 | 组件 | 说明 |
|------|------|------|
| 基础 | `DGlassButton` | 按钮 — 多变体/尺寸/Loading/Disabled/Outline |
| | `DGlassCard` | 卡片 — 核心容器，hoverable / glow |
| | `DGlassPanel` | 面板 — 带标题栏内容面板 |
| | `DGlassDivider` | 分割线 — 水平/垂直/文字插槽/发光 |
| 表单 | `DGlassInput` | 输入框 — 标签/清除/错误提示 |
| | `DGlassTextarea` | 文本域 — 多行输入 |
| | `DGlassSelect` | 选择器 — 下拉选择（Teleport + 动态定位） |
| | `DGlassSwitch` | 开关 — 切换控件 |
| | `DGlassSlider` | 滑块 — 数值滑动选择 |
| 数据展示 | `DGlassBadge` | 徽章 — 标记 + 圆点指示器 |
| | `DGlassTag` | 标签 — 多色可关闭标签 |
| | `DGlassAvatar` | 头像 — 文字/图片头像 + 状态 |
| | `DGlassProgress` | 进度条 — 多变体 |
| | `DGlassWidget` | 小组件 — VR 风格数据卡片 |
| | `DGlassTabs` | 标签页 — 内容切换 |
| | `DGlassTimeline` | 时间线 — 流程展示 |
| | `DGlassCollapse` | 折叠面板 — 手风琴模式 |
| | `DGlassLoading` | 加载 — Ring/Spinner/Skeleton |
| | `DGlassEmpty` | 空状态 — 无数据展示 |
| 反馈 | `DGlassAlert` | 警告提示 — 4 种类型 |
| | `DGlassDialog` | 对话框 — 模态弹窗（Teleport） |
| | `DGlassDrawer` | 抽屉 — 侧边滑出（Teleport + 独立过渡） |
| | `DGlassNotification` | 通知 — 消息通知条（Teleport） |
| | `DGlassTooltip` | 提示 — 悬浮文字提示 |
| | `DGlassPopover` | 弹出框 — 悬浮内容面板（Teleport + 动态定位） |
| 导航 | `DGlassNavbar` | 导航栏 — 顶部玻璃导航（z-index: 200） |
| | `DGlassBreadcrumb` | 面包屑 — 路径导航 |
| 特效 | `DBackgroundLayer` | 背景层 — 18 种动态/静态背景 |
| | `DBackgroundSwitcher` | 背景切换器 — 交互式浮窗控制器 |
| | `DDreamVeil` | 梦幻蒙版 — 日漫/P5R 白色柔光滤镜 |
| | `DBloomVeil` | 黑柔镜 — 高光扩散滤镜（Black Pro-Mist） |

## 十二、禁止事项

- ❌ 禁止使用纯色不透明背景（背景透明度不能超过 10%）
- ❌ 禁止省略 backdrop-filter
- ❌ 禁止省略多层 box-shadow（边框光晕是核心特征）
- ❌ 禁止使用尖锐直角（最小圆角 8px）
- ❌ 禁止省略 hover/focus 状态的光晕增强
- ❌ 禁止硬编码颜色值，必须使用 CSS 变量
- ❌ 禁止使用紫色/蓝色渐变作为页面背景
- ❌ 禁止在组件本体上使用彩色（彩色仅用于图标点缀）
- ❌ 禁止在浅色/白色背景上使用组件
- ❌ 禁止单层 box-shadow（至少 4 层才能实现正确的光晕效果）
- ❌ 禁止浮动弹出内容使用纯黑背景（必须 backdrop-filter 玻璃拟态）
- ❌ 禁止浮动元素 z-index 超过 Navbar（--dream-z-sticky: 200）
- ❌ 禁止新增组件不支持 GlassCustomProps（bgOpacity / borderColor / glowColor）
- ❌ 禁止新增组件不在 index.ts 和 lib.ts 中导出
